#include "main.h"
/**
 * _abs - compute the absolute value of an integer
 * @n: int to be checked
 * Return: the absolute int value
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
