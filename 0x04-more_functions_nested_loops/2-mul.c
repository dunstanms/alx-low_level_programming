#include "main.h"

/**
 * mul - multiplies two integers
 * @a: int to multiply to b
 * @b: int to multiply to a
 * Return: operation result
 */
int mul(int a, int b)
{
	int c;

	c = a * b;

	return (c);
}
